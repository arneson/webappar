'use strict';

/* Filters */

angular.module('PersonRegFilters', []).filter('startFrom', function() {
    return function(input, start) {
        // Not used but possible to create own filter.
    };
});

